<h3>Edit Profile</h3>

<%= form_for @user, html: {multipart: true} do |f| %>
  <hr>
  <%= f.label :name %>
  <%= f.text_field :name %>
  <%= f.label :image, "Edit Profile Image:" %>
  <%= f.cl_image_upload :image %>
  <br><br>
  <%= f.submit %>
<% end %>
